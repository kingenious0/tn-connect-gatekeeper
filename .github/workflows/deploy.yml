name: Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Sync & Reload
        run: |
          cd /home/azureuser/tn-connect-gatekeeper
          git pull origin main
          npm install --omit=dev
          pm2 reload gatekeeper
